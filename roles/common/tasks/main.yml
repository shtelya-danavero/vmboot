---
- name: Set a hostname
  ansible.builtin.hostname:
    name: test.dev.cloud

- name: Dafault packages
  ansible.builtin.apt:
    pkg:
      # - git
      # - wget
      # - awscli
      # - unzip
      # - mysql-client
      - apt-transport-https
      - software-properties-common
